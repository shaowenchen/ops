apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: script
  namespace: ops-system
spec:
  desc: run script
  variables:
    script:
      required: true
      display: script
      default: "uname -a"
  steps:
    - name: run script
      content: |
        ${script}
