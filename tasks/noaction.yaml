apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: noaction
  namespace: ops-system
spec:
  desc: no action to do
  host: anynode
  steps:
    - name: no action
      content: echo "no action to do"
