apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: list-node
  namespace: ops-system
spec:
  desc: use task `list-node` to list all **nodes** in specified Kubernetes cluster
  nodeName: anymaster
  steps:
    - name: list nodes
      content: kubectl get nodes | awk '{print $1 $2}'
