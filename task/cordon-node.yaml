apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: cordon-node
  namespace: ops-system
spec:
  desc: use task `cordon-node` to cordon node in cluster, variables must to set `node`
  nodeName: anymaster
  variables:
    node: ""
  steps:
    - name: cordon node
      content: kubectl cordon ${node}
