apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: cordon-node
  namespace: ops-system
spec:
  desc: use task `cordon-node` to cordon specified node within a Kubernetes cluster.
  nodeName: anymaster
  variables:
    node: ""
  steps:
    - name: cordon node
      content: kubectl cordon ${node}
