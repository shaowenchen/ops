apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: get-podlogs-bysvc
  namespace: ops-system
spec:
  desc: use task `get-podlogs-bysvc` to get pod logs in cluster, variables must to set `svc`, avaliable to set `topk`
  nodeName: anymaster
  variables:
    svc: ""
    topk: "50"
  steps:
    - name: get pod topk logs by svc name
      content: kubectl get svc --all-namespaces | grep ${svc} | awk '{print $1}' | xargs -L1 kubectl logs -l app=${svc} --tail=${topk} -n
