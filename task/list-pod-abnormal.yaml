apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: list-pod-abnormal
  namespace: ops-system
spec:
  desc: use task `list-pod-abnormal` to list **abnormal pods*** in specified namespace within a Kubernetes cluster
  nodeName: anymaster
  variables:
    namespace: default
  steps:
    - name: list abnormal pods
      content: kubectl get pod -n ${namespace} | grep -vE 'Running|Completed|Succeeded' |  awk '{print $1, $2, $5}'
