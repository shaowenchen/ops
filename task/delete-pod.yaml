apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: delete-pod
  namespace: ops-system
spec:
  desc: use task `delete-pod` to restart or delete pod in cluster, variables must to set `podname`、`namespace`
  nodeName: anymaster
  variables:
    podname: ""
    namespace: ""
  steps:
    - name: delete pod
      content: kubectl delete pod -n ${namespace} ${podname}
