name: Status Velero
variables:
  VeleroVersion: v1.9.2
steps:
  - name: Backup Status
    script: ./velero-${VeleroVersion}-linux-amd64/velero get backup
  - name: Restore Status
    script: ./velero-${VeleroVersion}-linux-amd64/velero get restore