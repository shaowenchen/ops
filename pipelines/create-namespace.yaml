apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: create-namespace
  namespace: ops-system
spec:
  desc: 创建 namespace
  variables:
    cluster:
      required: true
    namespace:
      required: true
  tasks:
    - name: 创建 namespace
      taskRef: create-namespace
