apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: app-argo-events
  namespace: ops-system
spec:
  desc: 安装、卸载 argo events 组件
  tasks:
    - name: argo-events
      taskRef: app-argo-events
