apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: clear-pod
  namespace: ops-system
  labels:
    ops/copilot: enabled
spec:
  desc: 清理 Pod
  tasks:
    - name: 清理 Pod
      taskRef: clear-pod
