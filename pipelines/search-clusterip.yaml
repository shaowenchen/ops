apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: search-clusterip
  namespace: ops-system
  labels:
    ops/copilot: enabled
spec:
  desc: 查看、搜索集群的 IP
  tasks:
    - name: search clusterip
      taskRef: search-clusterip
