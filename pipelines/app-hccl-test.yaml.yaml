apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: app-hccl-test
  namespace: ops-system
spec:
  desc: 安装、卸载 hccl-test 组件
  variables:
    cluster:
      required: true
    hccl-test-image:
      required: true
    action:
      required: true
      default: apply
  tasks:
    - name: hccl-test
      taskRef: app-hccl-test

